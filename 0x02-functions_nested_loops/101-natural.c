#include <stdio.h>
#include <stdio.h>
/**
 * main - entry point
 * natural
 * Return: Sucess
 */
int main(void)
{
	int sum, i;

	sum = 0;
	i = 0;
	do

	{
		if (i % 3 == 0)
		{
			sum = sum + i
		}
		if (i % 5 == 0)
		{
			sum = sum + i
		}
		i = i + 1;
	} while (i < 1024)
	return (0);
}
